<div class ="wrapper">
  <div id= "dating-background" class="col-lg-10 offset-2 pull-right text-center">
    <br><br>
      <div id="center"><p>
      <%= link_to "All Profiles", profiles_path, class: "btn btn-default btn-lg", id: "all-profiles-button" %>
      </p>
    </div>
      <br><br><br>
          <% @musicians.each do |musician| %>
            <div class="col-sm-6 col-md-4">
              <div class="thumbnail">
                <%= image_tag find_parent_profile(musician.profile_id).image.url(:medium) %>
                <div class="caption">
                  <h3><%= link_to find_parent_profile(musician.profile_id).name, profile_path(musician.profile_id) %></h3>
                  <h4><%= find_parent_profile(musician.profile.id).bio %></h4>
                    <%= musician.instrument %>
                    <%= musician.genre %>
                </div>
              </div>
            </div>
          <% end %> 
    <%= will_paginate @profiles %>
  </div>
</div>



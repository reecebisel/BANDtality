<section id="uber_background">
	<div class="container wrapper">
		<div>
			<div class="row">
				<h1 class="fjalla_font black_text"><b><%= @event.event_name %></b></h1>
					<div id="event_show_background" class="col-md-10">
					  <h2><%= image_tag @event.image.url(:medium) %></h2>
						<div class="whitesmoke">
							<h3 class="cantarell_font"><b>Date:</b> <%= @event.event_date.strftime("%d %B, %Y") %><br>
								 <b>Time:</b> <%= @event.start_time %><br>
								 <b>Venue:</b> <%= @event.event_venue %><br>
								 <b>Address:</b> <%= @event.event_address %><br>
								 <b>Tickets:</b> <%= @event.tickets %><br>
								 <hr></h3>
								 <p class="cantarell_font bigger_font"><%= @event.event_desc %></p>
						</div>
					</div>
			</div>
			<div class="row">
				<a class="nav_tags"><%= link_to("Back", events_path) %></a>
				<a class="nav_tags"><%= link_to("Edit", edit_event_path(@event.id)) %></a>
				<a class="nav_tags"><%= link_to("Delete", event_path(@event.id), method: :delete) %></a>
				<a class="nav_tags"><%= link_to("Add another event", new_event_path) %></a>
			</div>
		</div>

	</div>
</section>
